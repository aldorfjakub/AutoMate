# Must point at the same database file the backend uses.
DATABASE_URL=sqlite:../backend/project_db.sqlite

# Redis instance backing the job queue and live match streams.
REDIS_URL=redis://127.0.0.1:6379